We added a sequence number to all packet using struct. Packets are not written to the disc right away, instead it uses the variable ‘expected seq’ and a dictionary to store, momentarily,  the packets which are coming out of order. When the server receives the correct packet, it writes it to the file and then checks the buffer to see if the next packets are already waiting. Any duplicate or old packets are ignored.